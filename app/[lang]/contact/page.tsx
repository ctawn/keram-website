import { notFound } from "next/navigation"; import { EnquiryForm, Header } from "../../components"; import { copy, isLocale } from "../../data";
export default async function Contact({params}:{params:Promise<{lang:string}>}){const{lang}=await params;if(!isLocale(lang))notFound();const c=copy[lang];return <main><div className="inner-hero"><Header lang={lang}/><div><p className="eyebrow">{lang==="ar"?"تواصل معنا":"Contact"}</p><h1>{c.ctaTitle}</h1><p>{c.ctaBody}</p></div></div><section className="contact-wrap"><EnquiryForm lang={lang}/><div className="contact-note"><span>01</span><p>{lang==="ar"?"اترك طلبك وسيتواصل معك خبير سفر خلال يوم عمل.":"Leave a request and a travel specialist will contact you within one business day."}</p><span>02</span><p>{lang==="ar"?"رقم واتساب وتفاصيل المكتب ستُضاف بعد تأكيد بيانات النشاط.":"WhatsApp and office details will be added once business information is confirmed."}</p></div></section></main>}
